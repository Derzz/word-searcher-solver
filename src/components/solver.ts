const solver = function (gridString: string, wordString: string) {
    let grid: Array<Array<String>>;
    let words: Array<String>;
    console.log("thomas");
    //console.log(gridString);

    //TODO Steps
    // 1. Split gridString into grid, separate components as needed
    // 2. Split wordString into array of words
    // 3. Implement logic of searching for words one by one(aka brute force), we'll deal with bigger algorithm later
}

export default solver;
